line-length = 120
target-version = "py312"
extend-exclude = [".venv", "venv", "user_tools"]

[lint]
select = ["E", "F", "W", "I", "B"]
ignore = ["E501"]

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Security", "fastapi.params.Depends"]
